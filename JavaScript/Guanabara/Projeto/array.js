// let num = [a = 1, b = 2, c = 3, d = 4]
// num.push(e=5) //agrega em mais uma vaga
// num.length //mostra quantas posições tem o Array
// num.sort() //coloca em ordem crescente
// num.indexOf()//busca o valor da posição

//  console.log(`Nosso Vetor Array é o ${e}`)

 let valores = [8, 2, 3, 5, 7, 1];
 console.log(`Quantas posições ${valores.length}`)
 console.log(valores.length)

 
 //para declarar os valores de uma forma simples, usaremos o comando FOR.

//  for(let = pos=0; pos<valores.length; pos++) {
//   console.log(`A posição ${pos} tem o valor ${valores[pos]}`)
//  } 

 //podemos tbm usar de uma forma mais simplificada, usando o comando FOR(LET objeto in variavel).

//  for(let pos in valores);
//  console.log(`A posição ${pos} tem o valor ${valores[pos]}`)